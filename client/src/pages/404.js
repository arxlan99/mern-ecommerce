import React from "react";

const NotFound = () => {
  return (
    <div
      style={{
        flex: 1,
        textAlign: "center",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      404... Not Found Page
    </div>
  );
};

export default NotFound;
